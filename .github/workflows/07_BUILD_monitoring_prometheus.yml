name: 07 BUILD - monitoring_prometheus

on:
  push:
    paths:
      - 'src/monitoring_prometheus/**'
  workflow_dispatch: 

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Login to GitHub Packages Docker registry
      uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v2
      with:
        context: src/monitoring_prometheus
        push: true
        tags: ghcr.io/${{ github.repository }}/monitoring_prometheus:latest